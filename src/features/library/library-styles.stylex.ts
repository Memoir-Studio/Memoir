import * as stylex from "@stylexjs/stylex";
import { accents, colors, motion, typography } from "../../styles/tokens.stylex";

const fadeIn = stylex.keyframes({
  from: { opacity: 0 },
  to: { opacity: 1 },
});

const spin = stylex.keyframes({
  to: { transform: "rotate(360deg)" },
});

export const folderRowMarker = stylex.defineMarker();
export const outlineRowMarker = stylex.defineMarker();

export const sharedLibraryStyles = stylex.create({
  icon: { width: "16px", height: "16px" },
  iconSmall: { width: "14px", height: "14px" },
  fadeIn: {
    animationName: {
      default: fadeIn,
      "@media (prefers-reduced-motion: reduce)": "none",
    },
    animationDuration: motion.fast,
    animationTimingFunction: motion.ease,
  },
  spin: {
    animationName: {
      default: spin,
      "@media (prefers-reduced-motion: reduce)": "none",
    },
    animationDuration: "1s",
    animationIterationCount: "infinite",
    animationTimingFunction: "linear",
  },
  minWidth: { minWidth: 0 },
  truncate: {
    minWidth: 0,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
});

export const noteListStyles = stylex.create({
  panel: {
    display: "flex",
    width: "100%",
    maxWidth: "100%",
    height: "100%",
    minHeight: 0,
    minWidth: 0,
    flexDirection: "column",
    overflow: "clip",
    contain: "inline-size",
    borderRightWidth: "1px",
    borderRightStyle: "solid",
    borderRightColor: colors.border,
    backgroundColor: colors.panel,
  },
  header: {
    display: "flex",
    height: "56px",
    flexShrink: 0,
    alignItems: "center",
    justifyContent: "space-between",
    gap: "8px",
    paddingInline: "16px",
  },
  title: {
    color: colors.text,
    fontSize: "13px",
    fontWeight: 600,
    letterSpacing: 0,
  },
  headerSpacer: { width: "32px", height: "32px" },
  notes: {
    display: "flex",
    minHeight: 0,
    flex: 1,
    flexDirection: "column",
  },
  search: {
    position: "relative",
    display: "block",
    marginInline: "12px",
    marginTop: "10px",
  },
  searchIcon: {
    position: "absolute",
    top: "50%",
    left: "10px",
    width: "14px",
    height: "14px",
    color: colors.muted,
    pointerEvents: "none",
    transform: "translateY(-50%)",
  },
  searchInput: {
    height: "32px",
    paddingLeft: "32px",
    borderColor: {
      default: `color-mix(in srgb, ${colors.border} 54%, transparent)`,
      ":hover": `color-mix(in srgb, ${colors.border} 88%, transparent)`,
      ":focus": `color-mix(in srgb, ${colors.border} 88%, transparent)`,
    },
    borderRadius: "8px",
    backgroundColor: {
      default: `color-mix(in srgb, ${colors.elevated} 42%, ${colors.panel})`,
      ":hover": `color-mix(in srgb, ${colors.elevated} 72%, ${colors.panel})`,
      ":focus": `color-mix(in srgb, ${colors.elevated} 72%, ${colors.panel})`,
    },
    boxShadow: {
      default: "none",
      ":hover": "0 1px 4px rgb(72 62 48 / 4%)",
      ":focus": "0 1px 4px rgb(72 62 48 / 4%)",
    },
    fontSize: "12px",
    fontWeight: 450,
    letterSpacing: 0,
    "::placeholder": {
      color: `color-mix(in srgb, ${colors.muted} 82%, transparent)`,
      opacity: 1,
    },
  },
  countRow: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    padding: "12px 16px 8px",
    color: colors.muted,
    fontSize: "11px",
    fontWeight: 500,
  },
  compactButton: { width: "28px", height: "28px" },
  empty: {
    padding: "32px 12px",
    color: colors.muted,
    fontSize: "12px",
    textAlign: "center",
  },
  scroll: {
    display: "grid",
    flex: 1,
    alignContent: "start",
    gap: "6px",
    overflow: "auto",
    padding: "0 10px 12px",
  },
  virtualSpace: (height: number) => ({ position: "relative", height }),
  virtualRow: (top: number, height: number) => ({
    position: "absolute",
    insetInline: 0,
    top,
    height,
  }),
  card: {
    position: "relative",
    cursor: "pointer",
    borderColor: "transparent",
    borderRadius: "8px",
    backgroundColor: {
      default: "transparent",
      ":hover": `color-mix(in srgb, ${colors.elevated} 66%, transparent)`,
    },
    boxShadow: "none",
    transitionProperty: "background-color, border-color, box-shadow",
    transitionDuration: {
      default: "150ms",
      "@media (prefers-reduced-motion: reduce)": "0s",
    },
    transitionTimingFunction: motion.ease,
  },
  cardComfortable: { padding: "12px" },
  cardCompact: { padding: "10px 12px" },
  cardActive: {
    borderColor: "transparent",
    backgroundColor: `color-mix(in srgb, ${accents.soft} 72%, ${colors.panel})`,
    boxShadow: `inset 0 0 0 1px color-mix(in srgb, ${accents.primary} 8%, transparent)`,
  },
  cardMenuTarget: {
    backgroundColor: `color-mix(in srgb, ${colors.elevated} 78%, transparent)`,
    boxShadow: `inset 0 0 0 1px color-mix(in srgb, ${accents.primary} 16%, transparent)`,
  },
  cardDirty: {
    "::before": {
      position: "absolute",
      top: "10px",
      bottom: "10px",
      left: "-1px",
      width: "2px",
      borderRadius: "2px",
      backgroundColor: accents.primary,
      content: '""',
    },
  },
  cardHeader: {
    display: "grid",
    gridTemplateColumns: "auto minmax(0, 1fr) auto",
    alignItems: "center",
    gap: "8px",
  },
  cardIcon: { width: "14px", height: "14px", color: accents.primary },
  favoriteIcon: {
    width: "14px",
    height: "14px",
    fill: accents.primary,
    color: accents.primary,
  },
  cardTitle: {
    minWidth: 0,
    overflow: "hidden",
    color: colors.text,
    fontSize: "13px",
    fontWeight: 600,
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  excerpt: {
    display: "-webkit-box",
    marginTop: "6px",
    overflow: "hidden",
    color: colors.muted,
    fontSize: "11px",
    lineHeight: 1.65,
    WebkitBoxOrient: "vertical",
    WebkitLineClamp: 2,
  },
  metadata: {
    display: "flex",
    minWidth: 0,
    alignItems: "center",
    gap: "6px",
    marginTop: "6px",
  },
  tags: {
    display: "flex",
    minWidth: 0,
    flex: 1,
    alignItems: "center",
    gap: "6px",
    overflow: "hidden",
  },
  time: { flexShrink: 0, color: colors.muted, fontSize: "9px" },
});

export const outlineStyles = stylex.create({
  list: {
    display: "flex",
    width: "100%",
    minHeight: 0,
    minWidth: 0,
    flex: 1,
    flexDirection: "column",
    overflow: "auto",
    overflowAnchor: "none",
    overscrollBehavior: "contain",
    fontFamily: typography.uiFont,
    fontSynthesis: "none",
  },
  items: {
    display: "grid",
    minHeight: 0,
    flex: "1 1 auto",
    alignContent: "start",
    padding: "6px 10px 18px 6px",
  },
  item: (inset: number) => ({
    display: "grid",
    width: "100%",
    minWidth: 0,
    minHeight: "34px",
    gridTemplateColumns: "16px minmax(0, 1fr)",
    alignItems: "center",
    gap: "2px",
    padding: `0 12px 0 ${inset}px`,
  }),
  itemCompact: { minHeight: "28px" },
  toggleSlot: {
    display: "grid",
    width: "16px",
    height: "16px",
    flex: "none",
    placeItems: "center",
  },
  toggle: {
    padding: 0,
    borderWidth: 0,
    borderRadius: "4px",
    backgroundColor: {
      default: "transparent",
      ":hover": `color-mix(in srgb, ${colors.elevated} 80%, transparent)`,
      ":focus-visible": `color-mix(in srgb, ${colors.elevated} 80%, transparent)`,
    },
    color: {
      default: `color-mix(in srgb, ${colors.muted} 88%, ${colors.text})`,
      ":hover": colors.text,
      ":focus-visible": colors.text,
    },
  },
  chevron: {
    width: "12px",
    height: "12px",
    transitionProperty: "transform",
    transitionDuration: {
      default: "150ms",
      "@media (prefers-reduced-motion: reduce)": "0s",
    },
    transitionTimingFunction: motion.ease,
  },
  chevronOpen: { transform: "rotate(90deg)" },
  label: {
    display: "grid",
    width: "100%",
    minWidth: 0,
    minHeight: "34px",
    gridTemplateColumns: "3px minmax(0, 1fr)",
    alignItems: "center",
    gap: "5px",
    paddingBlock: "7px",
    borderWidth: 0,
    borderRadius: 0,
    backgroundColor: "transparent",
    color: {
      default: `color-mix(in srgb, ${colors.text} 38%, ${colors.muted})`,
      ":hover": colors.text,
      [stylex.when.ancestor(":hover", outlineRowMarker)]: colors.text,
    },
    fontFamily: typography.uiFont,
    fontSize: "13px",
    fontWeight: 400,
    lineHeight: 1.5,
    letterSpacing: 0,
    textAlign: "left",
    transitionProperty: "color",
    transitionDuration: {
      default: "150ms",
      "@media (prefers-reduced-motion: reduce)": "0s",
    },
    transitionTimingFunction: motion.ease,
  },
  labelDepthOne: {
    color: `color-mix(in srgb, ${colors.text} 86%, ${colors.muted})`,
    fontWeight: 500,
  },
  labelActive: { color: accents.primary, fontWeight: 500 },
  labelCompact: { minHeight: "28px", paddingBlock: "4px" },
  mark: {
    display: "block",
    width: "3px",
    height: "14px",
    borderRadius: "999px",
    backgroundColor: "transparent",
  },
  markActive: { backgroundColor: "currentColor" },
  text: {
    minWidth: 0,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  empty: {
    padding: "28px 12px",
    color: colors.muted,
    fontSize: "12px",
    textAlign: "center",
  },
});

export const linkStyles = stylex.create({
  emptyPanel: {
    display: "grid",
    flex: 1,
    placeItems: "center",
    paddingInline: "24px",
    color: colors.muted,
    fontSize: "12px",
    textAlign: "center",
  },
  panel: {
    display: "flex",
    minHeight: 0,
    flex: 1,
    flexDirection: "column",
    overflow: "auto",
    padding: "10px 12px 16px",
  },
  section: { marginBottom: "16px" },
  heading: {
    display: "flex",
    alignItems: "center",
    gap: "6px",
    marginBottom: "6px",
    paddingInline: "4px",
    color: colors.muted,
    fontSize: "11px",
    fontWeight: 500,
  },
  list: { display: "grid", gap: "4px", margin: 0, padding: 0, listStyle: "none" },
  empty: { paddingInline: "4px", color: colors.muted, fontSize: "11px", lineHeight: "20px" },
  row: {
    display: "grid",
    width: "100%",
    gridTemplateColumns: "16px minmax(0, 1fr)",
    alignItems: "start",
    gap: "8px",
    padding: "6px 8px",
    borderWidth: 0,
    borderRadius: "8px",
    backgroundColor: "transparent",
    color: "inherit",
    fontFamily: "inherit",
    textAlign: "left",
  },
  openable: {
    cursor: "pointer",
    backgroundColor: {
      default: "transparent",
      ":hover": `color-mix(in srgb, ${colors.elevated} 54%, transparent)`,
    },
  },
  rowIcon: { width: "14px", height: "14px", marginTop: "2px", color: colors.muted },
  rowTitle: {
    display: "block",
    overflow: "hidden",
    color: colors.text,
    fontSize: "12px",
    fontWeight: 500,
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  rowSubtitle: {
    display: "block",
    overflow: "hidden",
    color: colors.muted,
    fontSize: "10px",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
});

export const sidebarStyles = stylex.create({
  sidebar: {
    display: "flex",
    height: "100%",
    minHeight: 0,
    flexDirection: "column",
    borderRightWidth: {
      default: "1px",
      "@media (max-width: 760px)": 0,
    },
    borderRightStyle: "solid",
    borderRightColor: colors.border,
    backgroundColor: colors.panel,
    WebkitTextSizeAdjust: "none",
    textSizeAdjust: "none",
  },
  sidebarCollapsed: {
    width: {
      default: "auto",
      "@media (min-width: 761px)": "52px",
    },
    overflow: {
      default: "visible",
      "@media (min-width: 761px)": "hidden",
    },
  },
  navItem: {
    height: "34px",
    borderWidth: 0,
    borderRadius: "8px",
    backgroundColor: {
      default: "transparent",
      ":hover": `color-mix(in srgb, ${colors.elevated} 54%, transparent)`,
    },
    color: {
      default: `color-mix(in srgb, ${colors.text} 72%, ${colors.muted})`,
      ":hover": colors.text,
    },
    fontFamily: typography.uiFont,
    fontSize: "12px",
    fontWeight: 500,
    fontSynthesis: "none",
    lineHeight: 1,
    letterSpacing: 0,
    transitionProperty: "color, background-color, box-shadow",
    transitionDuration: {
      default: "150ms",
      "@media (prefers-reduced-motion: reduce)": "0s",
    },
    transitionTimingFunction: motion.ease,
  },
  navLayout: {
    display: "grid",
    width: "100%",
    gridTemplateColumns: "16px minmax(0, 1fr) auto",
    alignItems: "center",
    gap: "8px",
    paddingInline: "10px",
    textAlign: "left",
  },
  collapsedLayout: {
    gridTemplateColumns: {
      default: "16px minmax(0, 1fr) auto",
      "@media (min-width: 761px)": "minmax(0, 1fr)",
    },
    justifyItems: {
      default: "stretch",
      "@media (min-width: 761px)": "center",
    },
    gap: { default: "8px", "@media (min-width: 761px)": 0 },
    paddingInline: { default: "10px", "@media (min-width: 761px)": 0 },
  },
  active: {
    backgroundColor: `color-mix(in srgb, ${accents.soft} 76%, ${colors.panel})`,
    color: colors.text,
    fontWeight: 600,
    boxShadow: `inset 0 0 0 1px color-mix(in srgb, ${accents.primary} 9%, transparent)`,
  },
  navIcon: {
    display: "grid",
    width: "16px",
    height: "16px",
    flex: "none",
    placeItems: "center",
    overflow: "hidden",
  },
  navSvg: { width: "15px", height: "15px" },
  navIconActive: { color: accents.primary },
  folderIconColor: (color: string) => ({ color }),
  navLabel: {
    minWidth: 0,
    overflow: "hidden",
    lineHeight: 1.5,
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  collapsedHidden: {
    display: { default: "inline", "@media (min-width: 761px)": "none" },
  },
  navCount: {
    color: `color-mix(in srgb, ${colors.muted} 78%, transparent)`,
    fontSize: "10px",
    fontWeight: 400,
    fontVariantNumeric: "tabular-nums",
    lineHeight: 1,
    letterSpacing: 0,
  },
  navCountActive: { color: `color-mix(in srgb, ${colors.text} 54%, ${colors.muted})` },
  folderEmoji: {
    display: "grid",
    width: "16px",
    height: "16px",
    placeItems: "center",
    fontSize: "13px",
    lineHeight: 1,
  },
  folderEmojiColored: (color: string) => ({
    borderRadius: "5px",
    backgroundColor: `color-mix(in srgb, ${color} 18%, transparent)`,
  }),
  folderRow: {
    position: "relative",
    display: "grid",
    width: "100%",
    alignItems: "center",
  },
  folderTree: (depth: number) => ({
    paddingLeft: `${8 + depth * 10}px`,
    paddingRight: "8px",
    gap: "4px",
  }),
  folderWithToggle: { gridTemplateColumns: "14px minmax(0, 1fr) auto" },
  folderWithoutToggle: { gridTemplateColumns: "minmax(0, 1fr) auto" },
  folderActive: (color: string) => ({
    backgroundColor: `color-mix(in srgb, ${color} 16%, ${colors.panel})`,
    boxShadow: `inset 0 0 0 1px color-mix(in srgb, ${color} 18%, transparent)`,
  }),
  folderToggleSlot: {
    display: "grid",
    width: "14px",
    height: "14px",
    flex: "none",
    placeItems: "center",
  },
  folderToggle: {
    padding: 0,
    borderWidth: 0,
    borderRadius: "4px",
    backgroundColor: {
      default: "transparent",
      ":hover": `color-mix(in srgb, ${colors.elevated} 80%, transparent)`,
      ":focus-visible": `color-mix(in srgb, ${colors.elevated} 80%, transparent)`,
    },
    color: {
      default: `color-mix(in srgb, ${colors.muted} 88%, ${colors.text})`,
      ":hover": colors.text,
      ":focus-visible": colors.text,
    },
  },
  chevron: {
    width: "12px",
    height: "12px",
    transitionProperty: "transform",
    transitionDuration: {
      default: "150ms",
      "@media (prefers-reduced-motion: reduce)": "0s",
    },
    transitionTimingFunction: motion.ease,
  },
  chevronOpen: { transform: "rotate(90deg)" },
  navMain: {
    display: "grid",
    minWidth: 0,
    minHeight: "34px",
    alignItems: "center",
    gap: "6px",
    gridTemplateColumns: "16px minmax(0, 1fr)",
    padding: 0,
    borderWidth: 0,
    backgroundColor: "transparent",
    color: "inherit",
    fontFamily: "inherit",
    fontSize: "inherit",
    textAlign: "left",
  },
  navMainCollapsed: {
    gridTemplateColumns: {
      default: "16px minmax(0, 1fr)",
      "@media (min-width: 761px)": "minmax(0, 1fr)",
    },
    justifyItems: { default: "stretch", "@media (min-width: 761px)": "center" },
  },
  customize: {
    position: "absolute",
    top: "50%",
    right: "22px",
    display: "grid",
    width: "18px",
    height: "18px",
    placeItems: "center",
    padding: 0,
    borderWidth: 0,
    borderRadius: "5px",
    backgroundColor: {
      default: "transparent",
      ":hover": colors.elevated,
      ":focus-visible": colors.elevated,
    },
    color: { default: colors.muted, ":hover": colors.text, ":focus-visible": colors.text },
    opacity: {
      default: 0,
      ":hover": 1,
      ":focus-visible": 1,
      [stylex.when.ancestor(":hover", folderRowMarker)]: 1,
      [stylex.when.ancestor(":focus-within", folderRowMarker)]: 1,
    },
    transform: "translateY(-50%)",
    transitionProperty: "opacity, background-color, color",
    transitionDuration: {
      default: "150ms",
      "@media (prefers-reduced-motion: reduce)": "0s",
    },
    transitionTimingFunction: motion.ease,
  },
  customizeIcon: { width: "13px", height: "13px" },
  titlebar: {
    display: "flex",
    height: "48px",
    flexShrink: 0,
    alignItems: "center",
    justifyContent: "space-between",
    paddingInline: "12px",
  },
  titlebarTauri: {
    paddingLeft: { default: "12px", "@media (min-width: 761px)": "70px" },
  },
  titlebarCollapsed: {
    justifyContent: { default: "space-between", "@media (min-width: 761px)": "center" },
    // Override titlebarTauri's physical padding-left as well as the regular
    // titlebar inset. Mixing padding-inline with padding-left left the toggle
    // outside the 52px collapsed rail in some WebViews.
    paddingLeft: { default: "12px", "@media (min-width: 761px)": 0 },
    paddingRight: { default: "12px", "@media (min-width: 761px)": 0 },
  },
  brand: {
    display: "flex",
    minWidth: 0,
    alignItems: "center",
    gap: "8px",
    pointerEvents: "none",
  },
  brandCollapsed: {
    display: { default: "flex", "@media (min-width: 761px)": "none" },
  },
  logo: {
    display: "grid",
    width: "24px",
    height: "24px",
    flexShrink: 0,
    placeItems: "center",
    borderRadius: "7px",
    backgroundImage:
      "radial-gradient(circle at 28% 28%, #f3957f 0 12%, transparent 13%), linear-gradient(145deg, #343532 0 46%, #d65f4d 47% 100%)",
    boxShadow: "inset 0 0 0 1px rgb(255 255 255 / 12%), 0 2px 6px rgb(56 47 38 / 18%)",
    color: "#fffaf4",
    fontSize: "11px",
    fontWeight: 900,
  },
  brandName: {
    overflow: "hidden",
    color: colors.text,
    fontSize: "14px",
    fontWeight: 600,
    letterSpacing: 0,
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  collapseButton: { width: "32px", height: "32px", flexShrink: 0, alignSelf: "flex-start" },
  collapseButtonTauri: { marginTop: "7px" },
  collapseButtonCentered: {
    marginInline: { default: 0, "@media (min-width: 761px)": "auto" },
  },
  collapseButtonRight: { marginLeft: "auto" },
  scroller: { minHeight: 0, flex: 1, overflowY: "auto" },
  primaryNav: { display: "grid", gap: "4px", padding: "8px 10px" },
  group: {
    padding: "10px 10px 8px",
    borderTopWidth: "1px",
    borderTopStyle: "solid",
    borderTopColor: `color-mix(in srgb, ${colors.border} 44%, transparent)`,
  },
  sectionTitle: {
    display: "flex",
    height: "28px",
    alignItems: "center",
    justifyContent: "space-between",
    marginBottom: "4px",
    paddingInline: "10px",
    color: `color-mix(in srgb, ${colors.muted} 82%, transparent)`,
    fontFamily: typography.uiFont,
    fontSize: "10px",
    fontWeight: 500,
    lineHeight: 1,
    letterSpacing: 0,
  },
  sectionAction: {
    display: "grid",
    width: "18px",
    height: "18px",
    placeItems: "center",
    padding: 0,
    borderWidth: 0,
    borderRadius: "5px",
    backgroundColor: { default: "transparent", ":hover": colors.elevated },
    color: { default: colors.muted, ":hover": colors.text },
  },
  footer: {
    display: "flex",
    flexShrink: 0,
    alignItems: "center",
    gap: "6px",
    padding: "6px",
    borderTopWidth: "1px",
    borderTopStyle: "solid",
    borderTopColor: `color-mix(in srgb, ${colors.border} 72%, transparent)`,
    backgroundColor: `color-mix(in srgb, ${colors.panel} 92%, ${colors.elevated})`,
    fontFamily: typography.uiFont,
  },
  footerCollapsed: {
    flexDirection: { default: "row", "@media (min-width: 761px)": "column" },
    gap: { default: "6px", "@media (min-width: 761px)": "4px" },
    paddingBlock: { default: "6px", "@media (min-width: 761px)": "8px" },
  },
  footerActions: { display: "flex", flexShrink: 0, gap: "2px" },
  footerActionsCollapsed: {
    flexDirection: { default: "row", "@media (min-width: 761px)": "column" },
  },
  footerButton: { width: "28px", height: "28px" },
});

export const folderAppearanceStyles = stylex.create({
  dialog: { display: "grid", gap: "16px" },
  preview: {
    display: "grid",
    height: "36px",
    gridTemplateColumns: "16px minmax(0, 1fr)",
    alignItems: "center",
    gap: "8px",
    paddingInline: "12px",
    borderWidth: "1px",
    borderStyle: "solid",
    borderColor: `color-mix(in srgb, ${colors.border} 80%, transparent)`,
    borderRadius: "8px",
    backgroundColor: `color-mix(in srgb, ${colors.panel} 72%, ${colors.elevated})`,
    color: colors.text,
    fontSize: "13px",
    fontWeight: 600,
  },
  section: { display: "grid", gap: "8px" },
  label: {
    color: `color-mix(in srgb, ${colors.muted} 90%, ${colors.text})`,
    fontSize: "11px",
    fontWeight: 600,
    letterSpacing: 0,
  },
  emojiGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(8, minmax(0, 1fr))",
    gap: "4px",
  },
  emojiButton: {
    display: "grid",
    height: "32px",
    placeItems: "center",
    borderWidth: 0,
    borderRadius: "8px",
    backgroundColor: {
      default: `color-mix(in srgb, ${colors.panel} 70%, transparent)`,
      ":hover": `color-mix(in srgb, ${colors.elevated} 88%, ${colors.panel})`,
    },
    fontSize: "16px",
    lineHeight: 1,
    transitionProperty: "background-color, box-shadow",
    transitionDuration: {
      default: "150ms",
      "@media (prefers-reduced-motion: reduce)": "0s",
    },
    transitionTimingFunction: motion.ease,
  },
  emojiSelected: {
    backgroundColor: `color-mix(in srgb, ${accents.soft} 78%, ${colors.elevated})`,
    boxShadow: `inset 0 0 0 1px color-mix(in srgb, ${accents.primary} 22%, transparent)`,
  },
  fieldLabel: {
    display: "grid",
    gap: "7px",
    color: colors.muted,
    fontSize: "12px",
    fontWeight: 550,
    letterSpacing: 0,
  },
  swatches: { display: "flex", flexWrap: "wrap", alignItems: "center", gap: "8px" },
  swatch: (color: string) => ({
    display: "grid",
    width: "24px",
    height: "24px",
    placeItems: "center",
    padding: 0,
    borderWidth: "3px",
    borderStyle: "solid",
    borderColor: colors.elevated,
    borderRadius: "999px",
    backgroundColor: color,
    boxShadow: `0 0 0 1px color-mix(in srgb, ${color} 30%, ${colors.border})`,
    color: "#ffffff",
  }),
  swatchSelected: (color: string) => ({
    boxShadow: `0 0 0 2px ${colors.elevated}, 0 0 0 4px ${color}`,
  }),
  defaultSwatch: {
    display: "grid",
    width: "24px",
    height: "24px",
    placeItems: "center",
    padding: 0,
    borderWidth: "3px",
    borderStyle: "solid",
    borderColor: colors.elevated,
    borderRadius: "999px",
    backgroundColor: colors.elevated,
    backgroundImage: `linear-gradient(135deg, transparent 46%, color-mix(in srgb, ${colors.muted} 55%, transparent) 46% 54%, transparent 54%)`,
    boxShadow: `0 0 0 1px color-mix(in srgb, ${colors.border} 88%, transparent)`,
  },
  defaultSwatchSelected: {
    boxShadow: `0 0 0 2px ${colors.elevated}, 0 0 0 4px ${colors.muted}`,
  },
  swatchIcon: { width: "11px", height: "11px" },
  previewIcon: (color: string) => ({ color }),
  previewSvg: { width: "15px", height: "15px" },
});

export const indexStyles = stylex.create({
  inspector: {
    display: "flex",
    minHeight: 0,
    flex: 1,
    flexDirection: "column",
    overflow: "auto",
    padding: "10px 12px 16px",
    fontFamily: typography.uiFont,
    fontSynthesis: "none",
  },
  center: { display: "grid", flex: 1, placeItems: "center", color: colors.muted },
  error: { display: "grid", flex: 1, placeItems: "center", paddingInline: "16px", textAlign: "center" },
  errorIcon: { width: "20px", height: "20px", marginBottom: "8px", color: colors.muted },
  errorTitle: { color: colors.text, fontSize: "12px", fontWeight: 500 },
  retry: {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
    marginTop: "12px",
    padding: "6px 10px",
    borderWidth: 0,
    borderRadius: "8px",
    backgroundColor: "transparent",
    color: accents.primary,
    fontSize: "11px",
    fontWeight: 500,
  },
  hero: {
    padding: "12px",
    borderWidth: "1px",
    borderStyle: "solid",
    borderColor: `color-mix(in srgb, ${colors.border} 72%, transparent)`,
    borderRadius: "8px",
    backgroundColor: `color-mix(in srgb, ${colors.elevated} 78%, ${colors.panel})`,
  },
  heroRow: { display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: "8px" },
  path: {
    overflow: "hidden",
    color: colors.muted,
    fontFamily: typography.monoFont,
    fontSize: "11px",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  size: { marginTop: "4px", color: colors.text, fontSize: "13px", fontWeight: 600, letterSpacing: 0 },
  status: {
    display: "inline-flex",
    height: "20px",
    flex: "none",
    alignItems: "center",
    paddingInline: "8px",
    borderRadius: "999px",
    backgroundColor: `color-mix(in srgb, ${accents.soft} 78%, transparent)`,
    color: `color-mix(in srgb, ${colors.text} 82%, ${colors.muted})`,
    fontSize: "10px",
    fontWeight: 600,
    letterSpacing: 0,
    whiteSpace: "nowrap",
  },
  statusMemory: {
    backgroundColor: `color-mix(in srgb, ${colors.danger} 14%, ${colors.elevated})`,
    color: colors.danger,
  },
  statGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    gap: "8px",
    marginTop: "12px",
  },
  stat: {
    padding: "10px 12px",
    borderRadius: "8px",
    backgroundColor: `color-mix(in srgb, ${colors.elevated} 70%, ${colors.panel})`,
  },
  statValue: {
    color: colors.text,
    fontSize: "16px",
    fontWeight: 650,
    fontVariantNumeric: "tabular-nums",
    letterSpacing: 0,
    lineHeight: 1.1,
  },
  statLabel: { marginTop: "4px", color: colors.muted, fontSize: "10px", fontWeight: 500 },
  meta: { display: "grid", gap: "2px", marginTop: "12px", padding: "6px 2px" },
  row: {
    display: "grid",
    minHeight: "24px",
    gridTemplateColumns: "72px minmax(0, 1fr)",
    alignItems: "center",
    gap: "8px",
    fontSize: "12px",
  },
  rowLabel: { color: `color-mix(in srgb, ${colors.muted} 90%, ${colors.text})` },
  rowValue: {
    minWidth: 0,
    overflow: "hidden",
    color: colors.text,
    textAlign: "right",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  },
  callout: {
    padding: "8px 10px",
    borderRadius: "8px",
    backgroundColor: `color-mix(in srgb, ${accents.soft} 62%, ${colors.panel})`,
    color: `color-mix(in srgb, ${colors.text} 78%, ${colors.muted})`,
    fontSize: "11px",
    lineHeight: 1.5,
  },
  marginTop: { marginTop: "12px" },
  smallMarginTop: { marginTop: "8px" },
  hint: { marginTop: "12px", color: colors.muted, fontSize: "11px", lineHeight: "20px" },
  actions: { display: "grid", gap: "6px", marginTop: "12px" },
  dialogHint: { color: colors.muted, fontSize: "14px" },
});

export const libraryUtilityStyles = stylex.create({
  clipboardFallback: { position: "fixed", left: "-9999px" },
  favorite: { fill: accents.primary },
});
